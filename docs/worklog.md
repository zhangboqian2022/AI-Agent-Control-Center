# Worklog / 工作日志

> 追加式版本工作记录：每次发布或成规模的工作在本节下方新增一节（新者在前）。
> 内容：背景 → 做了什么 → 验证 → 遗留。设计/计划/评审细节见
> `docs/superpowers/`，逐任务台账见 `.superpowers/sdd/progress.md`（不入库）。

## 2026-09-04 — Qwen 百炼额度：同步优先登录与受保护自愈（1.4.6-rc.1）

**背景 / Background**：用户报告"登录成功后缓存很快掉、反复要求重新登录"。日志实锤
三件事：①隐藏 Chrome 呈现的会话自 8/29 构建后活不过 15 分钟（8 月同架构实测
5.5 小时）；②自动重复制在"单次横幅判定"下摧毁了 9 分钟前的新登录缓存；③登录
Chrome 成功后滞留 6 分钟不退出（Dock 运行态残留）+ 登录窗 14 秒被误判关窗。

**做了什么 / Changes**（分支 `fix/qwen-quota-sync-first-selfheal`，自 main
`2648177`，17 提交，逐任务 TDD+评审，全分支终审+复审后 fast-forward 合并）：

- 同步优先登录：点授权 = 先静默"普通取数→按来源恢复"安全链（含日常 Chrome
  会话同步），全程不弹窗；救不回才弹**居中**登录窗，且可见登录页同装反检测
  脚本（`7214ac4`、`444a555`；终审统一：`77b8124`）。
- 来源感知自愈：状态文件记 `session_origin`/`last_success_epoch`（v1 宽容扩展，
  缺省按手动登录保守迁移，`f87be3a`）；手动登录会话撞横幅先 60s 复检再允许
  重复制覆盖（`21c696f`）；不可信缓存自动降级免复检（`45ce21e`）。
- 自动弹窗：恢复耗尽自动开登录窗，1800s 限频、关掉即本次运行不再弹、主动
  登出永不弹（`6eb99df`、`77b8124`）。
- 健壮取消：登录窗取消判定改为"全部页面目标清零"，跨域短信/风控页不再 14 秒
  误关（`eba285b`）。
- 用完必退：操作结束按受管 profile 硬校验零进程、超时强杀、退出路径全打点
  （`4789e69`）。
- 文案/文档：额度条新增"正在同步/打开登录窗/自动恢复"双语态（`444a555`）；
  版本 1.4.6rc1 三件套+双语 CHANGELOG+release notes+KNOWN_LIMITATIONS 配对
  （`47e8cd2`）；README/用户指南中英双语补齐 Qwen 额度与任务发现的全部描述、
  下载锚点指向 rc.1（`905472c`、本次）。

**验证 / Verification**：全量 pytest 1610 通过/7 跳过；ruff/format/mypy 干净；
diff-cover 改动行 100%。真机：装机版 17:57 首启复现保护链（复检→确认→同步→
恢复），18:14 复现自动请求→同步失败 2.8s→居中回落；22:01–23:01 最终构建
每轮"横幅→即时同步→静默成功"，用户确认自动续期正常；发布 DMG 下载回环
sha256 与 hdiutil 校验通过。发布：GitHub Pre-Release `v1.4.6-rc.1`
（tag `905472c`，DMG+sha256 资产）。

**遗留 / Follow-ups**：①日常会话过期时自动弹窗"居中"的肉眼最终确认（单测已
覆盖计算与 CDP 序列）；②探针 T1–T4 定位隐藏呈现从 5.5h 掉到 <15min 的根因
（设计 §3.3，需用户配合）；③自动弹窗设置开关、`open_qwen_web_login` 的 UI
入口、retranslate 中间态措辞；④Windows 版 rc.1 资产待 Windows 机器补建。
