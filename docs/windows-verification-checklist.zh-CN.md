# Windows 真机冒烟清单 — AACC

只记录实际观察结果；未勾选项不代表兼容性承诺。

环境：Windows 10+，Python 3.12+，uv。

验证人：

验证日期与时间：

机器型号：

Windows 版本、版本号与内部版本：

Commit：

- [ ] **构建**：`scripts\build_windows.ps1` 执行成功，产物 `dist\AACC\AACC.exe` 存在
- [ ] **启动**：双击 `AACC.exe`，SmartScreen 提示后选择“更多信息 → 仍要运行”，面板出现并驻留托盘
- [ ] **发现会话**：运行中的 kimi / codex 会话被自动发现并显示状态灯
- [ ] **卡片聚焦**：右键“切换到任务”能聚焦目标终端窗口（窗口标题匹配）
- [ ] **按键注入**：白名单按键（Enter / Esc / 方向键 / Ctrl+C / 1 / 2）注入目标窗口生效
- [ ] **文本注入**：文本注入目标窗口内容正确
- [ ] **语音**：Win+H 语音输入在目标窗口可用
- [ ] **热键**：全局热键唤起面板（F13–F20 需 Fn 层映射的键盘确认映射后可用）
- [ ] **托盘**：面板最小化/隐藏后能从托盘恢复
- [ ] **额度条**：Kimi 额度条正确渲染（完成一次设备授权后）
- [ ] **设置页**：设置页各项（置顶、API 凭证重置等）可用且持久化
- [ ] **无任务时的 Codex 实时额度**：没有活动中或刚启动的 Codex 任务时，点击 Codex 额度条，确认只显示一行真实的 `WEEK` 百分比
- [ ] **行内绝对重置时间与 Kimi 顺序**：Codex 的本地重置日期时间直接显示在行内；Kimi 严格按 `5H`、`WEEK`、`MONTH` 排列，每个可用重置时间都在对应行内，不可用的月额度显示 `--`，不能显示成 `0%`
- [ ] **敏感文件 ACL**：保存设置并完成 Kimi 授权后，分别对 `config.yaml` 和 `kimi-credentials.json` 执行 `icacls`；确认已移除继承，显式完全控制权限仅授予当前用户 SID、Local System 和本机 Administrators
- [ ] **其他账户拒读**：登录另一个无特权本机账户，确认操作系统拒绝读取 `config.yaml` 和 `kimi-credentials.json`

证据 / 备注：

```text
在这里记录命令、实际观察结果、截图/日志位置和任何偏差。
```
